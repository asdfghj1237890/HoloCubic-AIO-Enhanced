name: Regression

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      mode:
        description: "compare (default) or update-golden (regenerate baselines as artifact)"
        type: choice
        default: compare
        options:
          - compare
          - update-golden

# Opt all JS-based actions in this workflow into Node.js 24 ahead of the
# 2026-06-02 forced default, which silences the per-action deprecation
# warning ("Node.js 20 actions are deprecated"). Once actions/checkout +
# friends ship a major release that bundles Node.js 24 explicitly, this
# block can come out and the action versions can be bumped instead.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  gui-regression:
    name: GUI regression (SDL2 host harness)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install SDL2 dev headers
        run: |
          sudo apt-get update
          sudo apt-get install -y libsdl2-dev

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      # Cache PIO global tools/platforms + the project's downloaded libs.
      # Massive speed-up on cache hit and avoids the GitHub-archive 502s
      # that bit us on the lv_drivers download (run #25070427104).
      - name: Cache PlatformIO data
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            lv_simulater_platformio/.pio
          key: pio-gui-${{ runner.os }}-${{ hashFiles('lv_simulater_platformio/platformio.ini') }}
          restore-keys: |
            pio-gui-${{ runner.os }}-

      # Retry with backoff to absorb transient GitHub-archive download
      # failures (502/503/timeouts) when PlatformIO fetches libs.
      - name: Build env:native_test
        working-directory: lv_simulater_platformio
        run: |
          set +e
          for attempt in 1 2 3; do
            pio run -e native_test
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "::notice::Build succeeded on attempt $attempt"
              exit 0
            fi
            if [ $attempt -lt 3 ]; then
              sleep_for=$((attempt * 15))
              echo "::warning::Build failed (rc=$rc) on attempt $attempt/3; retrying in ${sleep_for}s..."
              sleep $sleep_for
            fi
          done
          echo "::error::Build failed after 3 attempts"
          exit 1

      - name: Run scenario suite
        working-directory: lv_simulater_platformio
        env:
          SDL_VIDEODRIVER: dummy
        timeout-minutes: 5
        run: |
          set -e
          flags="--headless"
          if [ "${{ inputs.mode }}" = "update-golden" ]; then
            flags="$flags --update-golden"
          fi
          BIN=./.pio/build/native_test/program
          for scn in ../test/scenarios/*/*.scn; do
            echo "::group::scenario $scn"
            # Run without `if !` so $? reflects the real exit status (the `!`
            # form negates the pipeline status to 0/1, hiding rc=139).
            rc=0
            "$BIN" --scenario "$scn" $flags 2> stderr.log || rc=$?
            cat stderr.log
            if [ $rc -ne 0 ]; then
              # On segfault (rc=139) decode the +0xN frames via addr2line so
              # the trace shows file:line in the firmware/harness sources.
              if [ $rc -eq 139 ]; then
                echo "--- addr2line trace ---"
                grep -oE '\+0x[0-9a-f]+' stderr.log | sort -u | while read off; do
                  echo -n "$off  "
                  addr2line -Cfe "$BIN" "${off#+}" || true
                done
              fi
              exit $rc
            fi
            echo "::endgroup::"
          done

      # Always upload — review-time inspection of "actual" PNGs and any diff
      # PNGs is how human reviewers approve a UI change.
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-results
          path: test/results/**
          if-no-files-found: ignore
          retention-days: 14

      # Only meaningful when running in --update-golden mode; the freshly
      # written PNGs land here for the operator to download + commit.
      - name: Upload regenerated goldens
        if: always() && inputs.mode == 'update-golden'
        uses: actions/upload-artifact@v4
        with:
          name: regenerated-goldens
          path: test/golden/**
          if-no-files-found: ignore
          retention-days: 14

  # Track B: Unity unit tests over pure-logic firmware modules
  # (currently driver/imu.cpp's getAction). Runs in parallel with the
  # GUI regression job; a failure here blocks the PR independently.
  unit-tests:
    name: Unit tests (PlatformIO Unity, native)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      # Cache PIO global tools + AIO_Firmware_PIO's libdeps for the
      # native_unit env so transient archive 502s don't block the build.
      - name: Cache PlatformIO data
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            AIO_Firmware_PIO/.pio
          key: pio-unit-${{ runner.os }}-${{ hashFiles('AIO_Firmware_PIO/platformio.ini') }}
          restore-keys: |
            pio-unit-${{ runner.os }}-

      # Retry with backoff against transient download failures.
      - name: Run Unity tests (env:native_unit)
        working-directory: AIO_Firmware_PIO
        timeout-minutes: 5
        run: |
          set +e
          for attempt in 1 2 3; do
            pio test -e native_unit
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "::notice::Tests succeeded on attempt $attempt"
              exit 0
            fi
            if [ $attempt -lt 3 ]; then
              sleep_for=$((attempt * 15))
              echo "::warning::Tests failed (rc=$rc) on attempt $attempt/3; retrying in ${sleep_for}s..."
              sleep $sleep_for
            fi
          done
          echo "::error::Tests failed after 3 attempts"
          exit 1

      # Track C: ESP32FtpServer smoke tests. Separate env because the FTP
      # harness needs heavier stubs (String, IPAddress, scriptable
      # WiFiServer/SD) than the lean native_unit surface. Added as the
      # gating safety net for PR-3.3 (FtpServer file split).
      - name: Run FTP harness tests (env:native_ftp)
        working-directory: AIO_Firmware_PIO
        timeout-minutes: 5
        run: |
          set +e
          for attempt in 1 2 3; do
            pio test -e native_ftp
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "::notice::FTP tests succeeded on attempt $attempt"
              exit 0
            fi
            if [ $attempt -lt 3 ]; then
              sleep_for=$((attempt * 15))
              echo "::warning::FTP tests failed (rc=$rc) on attempt $attempt/3; retrying in ${sleep_for}s..."
              sleep $sleep_for
            fi
          done
          echo "::error::FTP tests failed after 3 attempts"
          exit 1

  # Track D: real ESP32 firmware compile (env:HoloCubic_AIO_Releases).
  # Catches the class of bug PR #71 missed — host stubs that diverge from
  # real Arduino-core headers (e.g. PR-3.3 split where WiFiServer was
  # only included via the host stub, not the firmware include chain).
  # Pre-this-job, that bug only surfaced at release-tag time.
  firmware-build:
    name: Firmware build (ESP32 release)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      # Cache the espressif32 platform tools + project libdeps. First run
      # downloads ~500MB of toolchain (~6-8 min); subsequent cache hits
      # cut it to ~2 min total.
      - name: Cache PlatformIO data
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            AIO_Firmware_PIO/.pio
          key: pio-firmware-${{ runner.os }}-${{ hashFiles('AIO_Firmware_PIO/platformio.ini') }}
          restore-keys: |
            pio-firmware-${{ runner.os }}-

      # Retry-with-backoff matches the other jobs; absorbs transient
      # GitHub-archive 502s on lib downloads.
      - name: Build firmware (env:HoloCubic_AIO_Releases)
        working-directory: AIO_Firmware_PIO
        timeout-minutes: 15
        run: |
          set +e
          for attempt in 1 2 3; do
            pio run -e HoloCubic_AIO_Releases
            rc=$?
            if [ $rc -eq 0 ]; then
              echo "::notice::Firmware build succeeded on attempt $attempt"
              exit 0
            fi
            if [ $attempt -lt 3 ]; then
              sleep_for=$((attempt * 15))
              echo "::warning::Firmware build failed (rc=$rc) on attempt $attempt/3; retrying in ${sleep_for}s..."
              sleep $sleep_for
            fi
          done
          echo "::error::Firmware build failed after 3 attempts"
          exit 1
