# AIO_Tool_rs (v3.0.0 work-in-progress)

This directory becomes `AIO_Tool/` at the v3.0.0 merge (`git rm -rf AIO_Tool/ && git mv AIO_Tool_rs AIO_Tool`).

Until then it lives alongside the legacy Python tool. See `Docs/superpowers/specs/2026-06-05-aio-tool-rust-rewrite-design.md` for design rationale and the plan series under `Docs/superpowers/plans/`.

Build: `cargo build --workspace`
Test:  `cargo test --workspace`
