# Smoke test for weather. WiFi never connects in the harness so the
# HTTP path is inert; captures the offline init state.

app weather

wait_ms 400
screenshot 01_initial
assert_no_crash

action RETURN
wait_ms 200
assert_no_crash
