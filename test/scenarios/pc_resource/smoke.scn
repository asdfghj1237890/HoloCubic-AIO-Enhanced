# Smoke test for pc_resource (PC system stats over Serial). No serial
# input arrives in the harness so values stay at init defaults.

app pc_resource

wait_ms 400
screenshot 01_initial
assert_no_crash

action RETURN
wait_ms 200
assert_no_crash
