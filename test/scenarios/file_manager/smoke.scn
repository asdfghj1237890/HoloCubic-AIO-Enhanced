# Smoke test for file_manager. FTP server starts up but never receives
# a real client in the harness; just verify boot + render + RETURN.

app file_manager

wait_ms 400
screenshot 01_initial
assert_no_crash

action RETURN
wait_ms 200
assert_no_crash
